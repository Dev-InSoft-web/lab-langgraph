## 2026-06-04 — Resumen (PatyIA)

- **TK-1433943:** Diligenciado — PG + MERGE staging ejecutados; **pendiente prueba AT** en chat Paty (tipos reportados en QA).
- **TK-1433968:** Obsolescencia `pmpt_*` en OpenAI — seguimiento documental (sin SQL en bitácora).

Relaciones débiles: el API anida detalle; si no hay enlace, el JSON no incluye ese bloque (sin FK obligatorias).

---

`Registrado por:` Jeff-Aporta
