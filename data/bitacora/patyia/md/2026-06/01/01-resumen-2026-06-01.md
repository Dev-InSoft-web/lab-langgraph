## 2026-06-01 — Resumen de trabajo (PatyIA)

- **Contexto:** Cierre y diligencia de los tres TK derivados de la reunión **Propuesta de mejora PatyIA** del 29-may-2026 ([YouTube `yXYP5yl88no`](https://www.youtube.com/watch?v=yXYP5yl88no), análisis en `2026-05/29/02-reunion-propuesta-mejora-patyia.md`). Incluye **TK-1431163** (composición OpenAI), **TK-1431662** (modelo por tipo) y **TK-1431666** (instrucciones Ultra en BD). Corrección de deploy CI por `openai-pricing.json` en UlMetrics.

- **Qué se hizo:**
  - Se validó la solución ya aplicada en `OpenIAServer.ts`: instrucciones por tipo vía `prompt.variables.instrucion_tipo` (sin `instructions` en el body que sobrescribía el template). Template PR_GENERAL v13 en OpenAI con slot `{{instrucion_tipo}}` al final.
  - Se diligenció el ticket en ISA-DOC con commits, tiempos (2 h) e ins/del.
  - Se corrigió `UlMetrics.ts` para cargar `openai-pricing.json` en runtime con fallback a costos en cero si el archivo no existe (gitignored); el build/deploy ya no depende de ese JSON.
  - Se eliminaron `TGroqController.ts` y `POST-Groq.ts` (código de prueba no usado en producción).
  - Se mapeó el repo `PatyIA` → `ISS-AyudasCPIA` en las URLs de commits del visor de tickets.

- **Archivos modificados (ISS-AyudasCPIA / PatyIA):**
  - `src/020 Controller/tools/UlMetrics.ts`
  - `.gitignore` (entrada obsoleta de POST-Groq)
  - Eliminados: `src/020 Controller/tools/TGroqController.ts`, `src/functions/POST-Groq.ts`

- **Archivos modificados (ISA-DOC):**
  - `src/lib/features/tickets/TK-1431163.ts`, `src/lib/features/tickets/index.ts`
  - `src/lib/features/tickets/template.ts` (alias repo PatyIA → ISS-AyudasCPIA)

- **Commits relacionados (ISS-AyudasCPIA):**
  - `a9808b7` · feat: Optimiza la gestión de conversaciones y prompts IA, integrando validación de esquemas, registro de métricas y utilidades de depuración.
  - `d4e3f75` · refactor: elimina comentarios innecesarios en el archivo UlMetrics.ts
  - `8a5bee3` · refactor: elimina comentario innecesario en el bloque try-catch de OpenIAServer.ts
  - `d8bf73b` · refactor(TK-1431163): elimina controlador TGroqController y ajusta carga de precios en UlMetrics

- **Estado:** **TK-1431163**, **TK-1431662** y **TK-1431666** diligenciados en ISA-DOC (cuerpo, commits PatyIA, cambios BD y tiempos). Deploy desbloqueado tras fix de UlMetrics (TK-1431163). Ultra en BD vía `seed-prompts-ultra-tdconsulta.sql` (TK-1431666). Pendientes menores documentados en bitácora 28-may: fallback de `nombre_usuario` sin JWT y persistencia de `itdconsulta` en conversación.

---

`Registrado por:` Jeff-Aporta
