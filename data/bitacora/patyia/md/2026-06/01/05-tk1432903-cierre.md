## TK-1432903 — Cierre técnico (log de peticiones)

### Implementado en PatyIA

| Entrega | Detalle |
|---------|---------|
| Archivo local | `logs/conversaciones/conv-{iconversacion}.json` (o `CONV_LOG_DIR`) |
| BD staging | `CONVERSACION_LOG` — `ICONVERSACION` + `CONTENT` (JSON mismo esquema) |
| Hook | Fin de cada turno SSE en `005 - OpenIAServer` → `appendConvTurno` |
| Fallos de stream | Turno también se persiste con `stream_ok: false` y `stream_error` |
| Métricas | Tokens, costo USD, latencia, operativas, `itdconsulta`, modelo, prompt_id, imágenes (`imagenes_count` en turno) |

### Consulta posterior (ISA-DOC)

- `src/pages/api/patyia/conversacion/[id]/log.ts`
- `readConvLog.ts` — merge archivo + `CONVERSACION_LOG`

### Pendiente operativo (fuera de código)

- Crear tabla en **producción** (`AYUDASCP_IA`) cuando se promueva el despliegue.
- Validación funcional con el **cliente V4** en ContaPyme (mismo contrato API; el log no depende del UI).

---

`Registrado por:` Jeff-Aporta · 2026-06-03
