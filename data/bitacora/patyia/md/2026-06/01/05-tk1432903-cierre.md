## TK-1432903 — Cierre técnico (log de peticiones + demo ISA PatyIA)

### Implementado en PatyIA (backend staging)

| Entrega | Detalle |
|---------|---------|
| BD staging | `CONVERSACION_LOG` — `ICONVERSACION` + `CONTENT` (JSON mismo esquema) |
| Hook | Fin de cada turno SSE en `OpenIAServer` → `appendConvTurno` (antes de `controller.close`) |
| Capa | `system/logs/UlMetrics.ts` + `UlConvLogDb.ts` (MERGE parametrizado) |
| Fallos de stream | Turno persistido con `stream_ok: false` y `stream_error` |
| Métricas | Tokens, costo USD, latencia, operativas, `itdconsulta`, modelo, prompt_id, imágenes |
| Lectura API | `GET /api/conversacion/{iconversacion}/log` |

### Demo operativa (GH Pages)

- URL: https://jeff-aporta.github.io/isa-patyia/
- Repo: `Jeff-Aporta/isa-patyia` (antes patyia-tools)
- Herramientas: **Visor de log** (por `iconversacion` o JSON pegado) y **Prompts → SQL** (editor 13 tipos + Guardar)
- Acceso asesora: usuario `VRESTREPO`, contraseña entregada por Teams, rol `instrucciones_editor`

### Consulta posterior

- ISA-DOC: `GET /api/patyia/conversacion/{id}/log` + `readConvLog.ts`
- Ticket diligenciado: `TK-1432903.ts` (tutoriales, capturas, commits, tiempos)

### Pendiente operativo

- Crear `CONVERSACION_LOG` en **producción** (`AYUDASCP_IA`) al promover despliegue V4.

---

`Registrado por:` Jeff-Aporta · 2026-06-10
