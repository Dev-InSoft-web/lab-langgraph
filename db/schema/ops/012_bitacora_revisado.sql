CREATE SCHEMA IF NOT EXISTS "BD_LAB";

CREATE TABLE IF NOT EXISTS "BD_LAB"."LAB_BITACORAREVISADO" (
	"REVISADOKEY" TEXT PRIMARY KEY,
	"BCHECKED" BOOLEAN NOT NULL DEFAULT false,
	"FHULTACT" TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS "IX_LAB_BITACORAREVISADO_CHECKED"
	ON "BD_LAB"."LAB_BITACORAREVISADO" ("BCHECKED")
	WHERE "BCHECKED" = true;
